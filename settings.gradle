rootProject.name = "avalon"

def String[] projects = [
        "service",
        "management"
]
include projects

projects.each {name ->
    def p = findProject(":${name}")
    p.name = "avalon-${name}"
    p.buildFileName = "${name}.gradle"
}
