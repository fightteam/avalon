rootProject.name = "avalon"

def String[] projects = [
        "service",
        "management",
        "portal"
]
include projects

projects.each {name ->
    def p = findProject(":${name}")
    p.name = "avalon-${name}"
    p.buildFileName = "${name}.gradle"
}
